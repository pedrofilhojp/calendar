apiVersion: v1
kind: Secret
metadata:
  name: agenda-secrets
  namespace: agenda
type: Opaque
stringData:
  # Em produção, gere valores únicos e use um gerenciador de segredos.
  DB_USER: agenda_user
  DB_PASSWORD: agenda_password
  RABBITMQ_USER: agenda_user
  RABBITMQ_PASSWORD: agenda_password
  JWT_SECRET: troque-este-segredo-em-producao
